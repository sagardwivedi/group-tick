import {
  Calendar,
  CheckCircle,
  CheckSquare,
  Users
} from "lucide-react";

export const icons = {
  Users,
  CheckSquare,
  Calendar,
  CheckCircle,
};

export type IconName = keyof typeof icons;
